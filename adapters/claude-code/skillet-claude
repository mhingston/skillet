#!/bin/sh
set -eu
: "${SKILLET_MCP_URL:=http://localhost:8080/mcp}"
: "${SKILLET_CLAUDE_SKILLS_DIR:=${HOME}/.claude/skills}"
exec skillet-adapter materialize -server "$SKILLET_MCP_URL" -candidate "$1" -destination "$SKILLET_CLAUDE_SKILLS_DIR" -harness claude
