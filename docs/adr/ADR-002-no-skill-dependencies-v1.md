# ADR-002: No skill dependencies in v1

Agent Skills has no standard dependency semantics. Every admitted skill must be independently materialisable. Cross-skill prose references are advisory only and never trigger recursive installation.
