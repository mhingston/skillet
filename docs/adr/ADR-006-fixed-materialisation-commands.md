# ADR-006: Fixed materialisation command templates

Acquisition commands are generated only from audited server templates and verify the package digest before extraction. No command is copied from skill content, metadata or repository files.
